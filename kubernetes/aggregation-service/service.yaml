apiVersion: v1
kind: Service
metadata:
  name: aggregation-service
  labels:
    app: aggregation-service
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: aggregation-service